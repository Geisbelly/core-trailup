[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "trailup-core"
version = "0.1.0"
description = "Medidas locais de estado do aluno para decidir quando vale chamar a LLM"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools]
packages = ["trailup_core"]
